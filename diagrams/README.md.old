# Діаграми для курсової роботи

Ця папка містить діаграми для курсової роботи "Habit Tracker - Веб-система для відстеження звичок та досягнень".

## 📊 Список діаграм

### 1. Архітектура системи (`1_architecture.png`)
**Опис:** Показує загальну архітектуру веб-системи, включаючи:
- Frontend компоненти (React)
- State management (Zustand)
- API клієнт (Axios)
- Backend шари (Controllers, Services, Repositories)
- Spring Security + JWT
- База даних MongoDB

**Призначення:** Розділ 3 або 4 - Проектування/Реалізація системи

---

### 2. Діаграма прецедентів (`2_usecase.png`)
**Опис:** Use Case діаграма, що показує:
- Актора системи (Користувач)
- Основні прецеденти використання:
  - Реєстрація та авторизація
  - Створення, редагування, видалення цілей
  - Логування прогресу
  - Перегляд статистики та досягнень
  - Робота з групами
  - Редагування профілю

**Призначення:** Розділ 3.5 - Моделювання прецедентів

---

### 3. ER Діаграма (`3_er_diagram.png`)
**Опис:** Entity-Relationship діаграма для MongoDB, що показує:
- Колекції: User, Goal, Progress, Group
- Поля кожної колекції з типами даних
- Зв'язки між колекціями (1:N відношення)
- Первинні ключі (PK) та зовнішні ключі (FK)

**Призначення:** Розділ 3.6 - Представлення даних ІС (модель БД)

---

### 4. Діаграма послідовності (`4_sequence.png`)
**Опис:** Sequence діаграма процесу автентифікації:
- Взаємодія між Користувачем, React App, AuthController, AuthService, MongoDB
- Крок за кроком показує процес login:
  1. Введення credentials
  2. HTTP запит POST /auth/login
  3. Перевірка в базі даних
  4. BCrypt verification
  5. Генерація JWT токенів
  6. Збереження в Zustand store
  7. Перенаправлення на Dashboard

**Призначення:** Розділ 2.2 - Алгоритм авторизації або Розділ 4 - Реалізація

---

### 5. Діаграма класів (`5_class_diagram.png`)
**Опис:** Class діаграма Backend частини системи:
- GoalController (з анотацією @Controller)
- GoalService (з анотацією @Service)
- GoalRepository (Interface, extends MongoRepository)
- Goal (Entity з полями та методами)
- Зв'язки між класами (uses, manages, persists)

**Призначення:** Розділ 4.1 - Опис реалізації backend частини

---

## 🛠️ Генерація діаграм

Діаграми створено автоматично за допомогою скрипта `generate_diagrams.py` з використанням бібліотеки PIL (Pillow).

### Як згенерувати діаграми заново:

```bash
python3 generate_diagrams.py
```

Скрипт створює 5 PNG зображень у папці `diagrams/`.

## 📝 Використання в документі

Діаграми можна вставити в Word документ наступним чином:

1. У Word виберіть місце для діаграми
2. Insert → Picture → This Device
3. Виберіть потрібний PNG файл
4. Додайте підпис під діаграмою (Insert → Caption)
5. Налаштуйте розмір за потреби

### Рекомендовані підписи:

- `Рисунок 1 - Архітектура системи Habit Tracker`
- `Рисунок 2 - Діаграма прецедентів системи`
- `Рисунок 3 - ER діаграма моделі даних MongoDB`
- `Рисунок 4 - Діаграма послідовності процесу автентифікації`
- `Рисунок 5 - Діаграма класів backend частини системи`

## 🎨 Технічні деталі

**Формат:** PNG  
**Розміри:**
- Архітектура: 1200x800
- Use Case: 1000x900
- ER діаграма: 1100x750
- Sequence: 1000x850
- Class діаграма: 1200x900

**Кольорова схема:**
- Синій (#3B82F6) - Controllers, Frontend
- Помаранчевий (#F59E0B) - Services
- Зелений (#10B981) - Repositories, Database
- Рожевий (#EC4899) - Entities

---

**Створено:** 11 грудня 2024  
**Автор:** Автоматична генерація для курсової роботи
